# Browser → API URL. With Docker Compose, defaults are set in infra/docker/docker-compose.yml
# (typically http://localhost:30880). For local `npm run dev` against compose API, use:
NEXT_PUBLIC_API_BASE_URL=http://localhost:30880
